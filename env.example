# For Vercel/cloud persistence, set these environment variables.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
SESSION_SECRET=replace-with-a-long-random-secret

# Local development can omit DATABASE_URL; the app uses data/db.json automatically.
